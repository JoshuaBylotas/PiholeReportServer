{
  "AzureAd": {
    "Instance": "https://login.microsoftonline.com/",
    "Domain": "REPLACE_WITH_YOUR_DOMAIN.onmicrosoft.com",
    "TenantId": "REPLACE_WITH_TENANT_GUID",
    "ClientId": "REPLACE_WITH_APP_CLIENT_ID",
    "CallbackPath": "/signin-oidc",
    "SignedOutCallbackPath": "/signout-callback-oidc",

    "//ClientSecret": "NEVER commit this. Set it with 'dotnet user-secrets set AzureAd:ClientSecret <value>' in development, or the environment variable AzureAd__ClientSecret in production. Prefer a certificate or managed identity over a secret.",

    "//RequireAppRoles": "Set true once Report.Viewer / Report.SqlAuthor app roles are assigned in Entra ID. While false, any user in the tenant who can sign in is treated as a viewer.",
    "RequireAppRoles": false
  },

  "Sql": {
    "Server": "REPLACE_WITH_SQL_HOST",
    "Port": 1433,
    "Database": "pihole",

    "//AuthMode": "SqlLogin | Integrated | EntraApp | EntraOnBehalfOf. See docs/03-sql-server-setup.md — the Entra modes require Azure SQL, SQL Managed Instance, or an Azure Arc-enabled SQL Server 2022+.",
    "AuthMode": "SqlLogin",

    "//UserId": "Set via user-secrets or the Sql__UserId environment variable. Must be a login with db_datareader only.",

    "TokenScope": "https://database.windows.net//.default",
    "Encrypt": true,
    "//TrustServerCertificate": "Set true only for an on-premises instance presenting a self-signed certificate.",
    "TrustServerCertificate": false,
    "ConnectTimeoutSeconds": 15,
    "CommandTimeoutSeconds": 60,
    "ApplicationName": "PiholeReportServer"
  },

  "Ai": {
    "//": "Local inference for natural-language querying and result summaries. Runs on the LAN so DNS history never leaves the network. Endpoint is an Ollama server, e.g. http://<AI_HOST>:11434",
    "Enabled": false,
    "Endpoint": "",
    "Model": "qwen2.5-coder:3b",
    "//FallbackEndpoint": "Second inference host, used ONLY when Endpoint cannot be reached at all. The preferred host is a GPU machine that is not always on the network; without this the AI features and the nightly job go dark whenever it is away. Failover never triggers on a timeout or an HTTP error - those mean a server answered, and the standby is the slower machine.",
    "FallbackEndpoint": "",
    "//FallbackModel": "Model to ask for on the standby, usually smaller. Defaults to Model when empty.",
    "FallbackModel": "",
    "//FallbackRetryPrimarySeconds": "How long to keep using the standby before retrying the preferred host. Clamped to 15-3600. Recovery is automatic; nothing needs restarting.",
    "FallbackRetryPrimarySeconds": 120,
    "//TimeoutSeconds": "CPU-only hosts generate a few tokens per second; a question can legitimately take 30-60s.",
    "TimeoutSeconds": 180,
    "//MaxSummaryRows": "Rows considered when building the statistical digest that is narrated. The digest is computed locally, so this is cheap.",
    "MaxSummaryRows": 5000,
    "Temperature": 0.1,
    "MaxOutputTokens": 800,
    "//MaxAgentSteps": "Queries the analysis agent may run for one question. Each step is a generation plus a database round-trip.",
    "MaxAgentSteps": 4,
    "AgentBudgetSeconds": 300,
    "//NightlyAnalysisEnabled": "Runs the standing battery of questions once a night and stores the findings. Separate from Ai:Enabled so interactive features can run without the unattended job.",
    "NightlyAnalysisEnabled": false,
    "NightlyAnalysisHour": 3
  },

  "Reporting": {
    "//MaxRows": "Rows a single query may materialise. Held server-side and paged, so this is bounded by worker memory, not by what a browser can render.",
    "MaxRows": 100000,
    "//PageSize": "Rows sent to the browser at a time. Only this many are ever rendered.",
    "PageSize": 100,
    "MaxExportRows": 250000,
    "ReportsPath": "Reports",
    "DefaultRangeDays": 7,
    "EnableRawSql": true
  },

  "DataProtection": {
    "//KeyPath": "Where to persist the data protection key ring. Set this in production - for example C:/ProgramData/PiholeReportServer/keys - so the auth cookie survives an app pool recycle and a redeploy. Leave empty in development, where the framework default is fine.",
    "KeyPath": ""
  },

  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning",
      "Microsoft.Identity": "Warning"
    },
    "//EventLog": "The site runs under IIS, where ILogger output otherwise goes nowhere. Kept at Warning by default: an event per request would bury the failures the log exists to surface. The app's own categories stay at Information so a query, an AI call or a nightly run is still recorded.",
    "EventLog": {
      "LogLevel": {
        "Default": "Warning",
        "PiholeReportServer": "Information"
      }
    }
  },

  "AllowedHosts": "*"
}
